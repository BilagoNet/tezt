# INTENTIONAL SyntaxError: this file must NOT compile.
# Collecting/importing it produces a collection ERROR outcome (not a failure).
# Do not "fix" this file; py_compile is expected to reject it.

def test_broken(:
    return None
